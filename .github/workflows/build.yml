name: Build

on:
  push:
  pull_request:
  workflow_dispatch:
  schedule:
    - cron:  '0 0 * * *'

jobs:
  build:
    runs-on: windows-latest
    strategy:
      matrix:
        platform:
          - { mingw: i686, bits: 32 }
          - { mingw: x86_64, bits: 64 }
        build_type:
          - release
          - debug
#        include:
#          - { platform: i686, suffix: 32, build_type: release }
#          - { platform: x86_64, suffix: 64, build_type: release }
#          - { platform: i686, suffix: 32, build_type: debug }
#          - { platform: x86_64, suffix: 64, build_type: debug }
    steps:
      - uses: actions/checkout@v2

      - uses: msys2/setup-msys2@v2
        with:
          msystem: mingw64

      - name: Install dependencies
        shell: msys2 {0}
        run: |
          pacman -S --needed --noconfirm\
            mingw-w64-${{matrix.platform.mingw}}-make
            mingw-w64-${{matrix.platform.mingw}}-gcc

      - name: Checkout sources
        shell: msys2 {0}
        run: |
          git clone --depth https://github.com/bkaradzic/bgfx.git
          git clone --depth https://github.com/bkaradzic/bx.git
          git clone --depth https://github.com/bkaradzic/bimg.git

      - name: Compile BX
        shell: msys2 {0}
        run: |
          cd bx
          make mingw-gcc-${{matrix.build_type}}${{matrix.platform.bits}}

      - name: Compile BIMG
        shell: msys2 {0}
        run: |
          cd bimg
          make mingw-gcc-${{matrix.build_type}}${{matrix.platform.bits}}


      - name: Compile BGFX
        shell: msys2 {0}
        run: |
          cd bgfx
          make mingw-gcc-${{matrix.build_type}}${{matrix.platform.bits}}